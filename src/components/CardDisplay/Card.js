import React from "react";
import styled from "styled-components";

const CardComponent = styled.div`
  img {
  }
`;
const Card = () => {
  return (
    <CardComponent>
      <img></img>
    </CardComponent>
  );
};

export default Card;
